binary search
	
	R0 is counter
	R1 is the element we are searching for
	R2 is the current value of the array[counter] (R3) 
	R3 is the array
	
	
main 
	
	
	
	MOV R0, #0
L1: ADD R0, R0, #1
	LDR R2,[R3,R0,//LSL #2 ??]
	CMP R2, R1
	BNE L1
	MOV Index, R0
	
	int x=0;
	while(A[x] != element)
	{
		x++;
	}
	index = x;